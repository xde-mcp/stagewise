pre-commit:
  parallel: true
  jobs:
    - name: check
      run: biome check --write --unsafe --staged --no-errors-on-unmatched
      glob: "*.{js,ts,jsx,tsx}"

commit-msg:
  parallel: true
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit $1

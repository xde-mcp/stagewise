pre-commit:
  parallel: true
  jobs:
    - name: check
      run: biome check --write --unsafe --staged     
      glob: "*.{js,ts,jsx,tsx}"

commit-msg:
  parallel: true
  jobs:
    - name: commitlint
      run: pnpm commitlint --edit $1
